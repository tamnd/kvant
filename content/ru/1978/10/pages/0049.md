---
issue: kvant_1978_10
year: 1978
number: "10"
page_index: 49
page_label: "47"
lang: ru
source: kvant_digital
source_scan: kvant_1978_10
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 8890ccfa86b1edb22843747e5e5f47f4a63c75aa48e428e5a326b570e5d1f43a
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 47⟧

Рис. 9.

Рис. 10.

Рис. 11.

Рис. 12.

была бы не меньше 108° + 2·60° > 180°). Таким образом многоугольники разбиения могут применять только к несмежным сторонам хорошего прямоугольника. Пусть $[AB]$ — сторона пятугольника $I$, к которой применяют еще какие-то многоугольники разбиения (рис. 8). Пусть $A_1$ и $B_1$ — вторые концы сторона многоугольника $M$, выходящих из вершин $A$ и $B$ соответственно ($A_1$ и $B_1$ — не вершины пятугольника $I$; возможно, что $A_1 = B_1$). Обозначим через $C$ точку пересечения $(AA_1)$ с $(BB_1)$. Тогда $\triangle ACB$ — правильный треугольник, внутри которого расположены все многоугольники разбиения, лежащие выше $[AB]$ (если $\Pi$ расположен под $[AB]$, как на рисунке 8).

6°. Докажем, что все многоугольники разбиения, лежащие внутри $\triangle ABC$ (рис. 8), являются треугольниками.

Предположим противное — пусть есть «не-треугольники». (Будем считать, что сторона $[AB]$ горизонтальная.) Рассмотрим ближайшую к $[AB]$ (самую низку) вершину не-треугольника; если таких вершин несколько, то возьмем самую правую средние них. Обозначим эту «самую низку» и самую правую» вершину через $D$. Точка $D$ не может лежать на стороне $[AB]$: если «не-треугольник» расположен так, как на рисунке 9, $a$, то один из углов не больше $\frac{180° - 90°}{2} = 45°$ — «плохой»; если так, как на рисунке 9, $b$, и «не-треугольник» — «не-квадрат», то снова получается «плохой» угол (мы условились, что «не-треугольник» — и «не шестугольник»); если же «не-треугольник» — квадрат, то справа к вершине $D$ применяет снова квадрат (рис. 9, $\theta$), и, значит, вершина $D$ — не самая правая. Точка $D$ не может лежать на сторонах $[AA_1]$ и $[BB_1]$ многоугольника $M$: концы $A_1$ и $B_1$ — не ближайшие к $[AB]$, а если $D$ — внутриняя точка $[AA_1]$ или $[BB_1]$, то тогда «не-треугольник» должен быть целиком расположен внутри угла в 60° (рис. 10, $a$, $\theta$), что невозможно.

Итак, точки $D$ — внутриняя точка области $AA_1 \dots B_1B$ многоугольника $M$. Поскольку все «не-треугольники», сходящей в $D$, должны расположаться не ниже углов при вершине $D$ не больше 180°. Значит, это либо два квадрата, либо с вершиной в $D$ есть только один «не-треугольник». Если это два квадрата, то поскольку только горизонтально — так, как на рисунке 11; но тогда вершина $D$ — не самая правая вершин «не-треугольников». Если же «не-треугольник» в $D$ только один (например, $k > 4$, $k \neq 6$), а остальные многоугольники разбиения — треугольники, то тогда

$$360^\circ - 180^\circ \left(1 - \frac{2}{k}\right) = 60^\circ n$$

(n — натуральное). Отсюда $k = \frac{6}{n - 3}$. В натуральных числах это уравнение имеет также решение: $n = 4, k = 6$;
