---
issue: kvant_1982_4
year: 1982
number: "4"
page_index: 6
page_label: "4"
lang: ru
source: kvant_digital
source_scan: kvant_1982_4
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 8680b527400f8cd05517e2353ce3a24de35a431114fd5f781586164254c82b61
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 4⟧

движения, а не та линия, которую описывает движущая точка. Однако и ту же линию можно описать разными способами.

В процессе движения точка $z(t)$ в различных моменты времени может попадать в одну и ту же точку плоскости, так что не исключается равенство

$$z(t_2) = z(t_3)$$ при $t_2 \neq t_3$.

Таким образом, пусть может иметь самопересечения. Он может даже состоять из одной точки, именно в случае, когда точка $z(t)$ вовсе не перемещается при изменении $t$.

В дальнейшем, если это не будет оговорено специально, мы всегда будем предполагать, что пусть не проходит через начало координат, то есть что величина $z(t)$ ни при каком значении $t$ не обращается в нуль.

Toчка $z(t_0) = z_0$ называется началом пути, а точка $z(t_1) = z_1$ — его концом.

Если имеет место равенство $z_0 = z_1$, то пусть называется замкнутым (рис. 2).

Так как комплексно число $z(t)$ не обращается в нуль, для всякого значения $t$ определяен аргумент $\varphi(t)$ комплексно числа $z(t)$; но он определяен лишь с точностью до слагаемого $2k\pi$. Эта неоднозначность для нас не желательна. Для того чтобы освободиться от нее, мы выберем для начальной точки $z_0$ вполне определенный аргумент $\varphi_0 = \varphi(t_0)$.

Затем по мере возрастания $t$ будем выбирать аргумент $\varphi(t)$ точки $z(t)$ так, чтобы при малых изменениях $t$ он менялся мало. Этот неоднозначность выбора аргумента будет устранена. Добавление к аргументу числа $2k\pi$ при $k \neq 0$ приводило бы сразу к резкому изменению величины $\varphi(t)$. Выбрав начальное значение аргумента $\varphi(t_0) = \varphi_0$ и следя за тем, чтобы аргумент $\varphi(t)$ точки $z(t)$ менялся вместе с $t$ непрерывно, мы получаем вполне определенную функцию $\varphi(t)$, меняющую непрерывно, то есть без скачков. Если выбрать начальное значение аргумента $\varphi_0$ иначе, изменив его на $2k\pi$, то он будет отличаться от ранее выбранногоровно на $2k\pi$ на всем протяжении изменения $t$. Отсюда следует, что при таком способе построения функции $\varphi(t)$ величина

$$\varphi(t_1) - \varphi(t_0)$$

не зависит от случаяно выбранного начального значения аргумента числа $z_0$.

Если пусть замкнут, то точки $z_0$ и $z_1$ совпадают и, следовательно, их аргументы $\varphi(t_0)$ и $\varphi(t_1)$ могут отличаться лишь на $2k\pi$. Поэтому число (1) в случае замкнутого пути есть $2k\pi$. Целое число $k$ называется индексом замкнутого пути в плоскости комплексно первогоного $z$. Следует еще раз подернуть, что индекс замкнутого пути можно определить лишь в том случае, когда пусть не проходит через начало координат.

Индекс $k$ имеет простой геометрический смысл. Именно, он указывает, сколько раз точка $z(t)$, описывая замкнутый пути, обходит начало координат (рис. 3).

Рассмотрим простой пример. Пусть $1 + r(\cos t + i \sin t); 0 \leq t \leq 2\pi$ является замкнутым. Он описывает окружность с центром в точке 1 и радиусом $r$ и проходит окружность
