---
issue: kvant_1975_1
year: 1975
number: "1"
page_index: 33
page_label: "31"
lang: ru
source: kvant_digital
source_scan: kvant_1975_1
extraction: vision
extraction_model: claude
content_sha256: c00bed0626887d1a845dc6bdf8bfec296a427c05bbcf4a717625265160960be6
prompt_sha256: 43ebaab4f4a6e405497f70d0085544cc98775efb1d69ff4d84e8ff4765aaaf8e
---

ме синусов этих углов? Суть этой «детской» ошибки в том, что школьник, познакомившись с какой-либо новой функцией — назовем ее $\Phi$,— склонен считать, что она должна обладать следующим «простым» свойством:

$$\Phi(x_1 + x_2) = \Phi(x_1) + \Phi(x_2). \qquad (1)$$

Линейная однородная функция этим свойством действительно (проверьте!) обладает, но, увы, им *не обладают другие функции, изучаемые в школе*.

Всякую функцию, обладающую свойством, выраженным равенством (1), называют *аддитивной* функцией. Легко проверить, что *линейная неоднородная* функция, то есть функция $G$, определенная для любого вещественного значения аргумента формулой

$$G(x) = ax + b \qquad (b \neq 0),$$

уже не является аддитивной; точно так же не обладают свойством аддитивности и функции, определенные для любого значения аргумента $x$, значения которых определяются по следующим правилам:

$$x \to ax^2 + bx + c,$$
$$x \to \frac{1}{x^2 + a^2},$$

и многие, многие другие функции.

Да существует ли хоть одна какая-нибудь аддитивная функция от вещественного аргумента, отличная от линейной однородной,— спросит читатель.

Ответ на этот вопрос он получит, если не поленится внимательно прочитать эту небольшую статью.

**2. Исследования произвольной аддитивной функции**

1. Если функция $\Phi$ обладает свойством (1), то, как легко проверить, она обладает и следующим свойством:

$$\Phi(x_1 + x_2 + x_3) =$$
$$= \Phi(x_1) + \Phi(x_2) + \Phi(x_3). \quad (1'),$$

В самом деле, в силу свойства (1)

⟦column⟧

имеем

$$\Phi(x_1 + x_2 + x_3) = \Phi((x_1 + x_2) + x_3) =$$
$$= \Phi(x_1 + x_2) + \Phi(x_3) =$$
$$= \Phi(x_1) + \Phi(x_2) + \Phi(x_3).$$

Читателя не затруднит доказать, что и

$$\Phi(x_1 + x_2 + \ldots + x_n) =$$
$$= \Phi(x_1) + \Phi(x_2) + \ldots + \Phi(x_n) \qquad (1'')$$

(придется, конечно, воспользоваться методом математической индукции).

Из формулы $(1'')$ следует равенство

$$\Phi(nx) = n\Phi(x) \qquad (2)$$

($n$ — произвольное *целое* положительное число).

Так как равенство (2) справедливо при любом значении $x$, положим в нем $x$ равным $\frac{m}{n} y$ ($m$ и $n$ — целые положительные). Получим

$$\Phi(my) = n\Phi\left(\frac{m}{n} y\right).$$

Вновь используя равенство (2), получим

$$m\Phi(y) = n\Phi\left(\frac{m}{n} y\right),$$

то есть

$$\Phi\left(\frac{m}{n} y\right) = \frac{m}{n} \Phi(y). \qquad (2')$$

Итак, мы убедились в том, что если функция $\Phi$ аддитивна, то для любого положительного рационального числа $\alpha = \frac{m}{n}$ имеет место равенство

$$\Phi(\alpha x) = \alpha\Phi(x) \qquad (3)$$

(каково бы ни было вещественное число $x$!).

Это замечательное соотношение дает нам *возможность вычислить для любой аддитивной функции $\Phi$ ее значение для любого положительного рационального значения $x$ аргумента, если задано только одно ее значение — а именно то, которое она принимает, когда аргумент равен единице.*

В самом деле, положив в (3) $x$ равным единице, мы получим

$$\Phi(\alpha) = \alpha\Phi(1). \qquad (4)$$

⟦folio 31⟧
