---
issue: kvant_1988_11-12
year: 1988
number: 11-12
page_index: 25
page_label: "23"
lang: ru
source: kvant_digital
source_scan: kvant_1988_11-12
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 2107e22f9e4c47e630dc9db3bbf43cdda7888563d3cb35c6a7c390f5213728bb
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 23⟧

Позному формулы $(x, y) = (1, 0) + (2, 1) + \dots + (q-1, q-2) + (m+q, m+q-1)$ при $q > 0$ и $(m, m) = (1, 0) + (m-1, m)$ при $q = 0$, $m > 0$ дают представления $(x, y)$ в виде суммы различных образующих.

Доказать необходимость условия тоже несложно. Пусть
$x = r_1 + \dots + r_a + s_1 + \dots + s_b$
$y = r_1 + \dots + r_a - a + s_1 + \dots + s_b + b$
позному $x - y = a - b$. Положим $q = x - y$ и
$m = (r_1 - q) + (r_2 - (q - 1)) + \dots$
$\dots + (r_q - 1) + r_{q+1} + \dots + r_a +$
$+ s_1 + \dots + s_b = x + y - q(q + 1)/2$
(здесь мы снова воспользовались формулой суммы арифметической прогрессии). Из неравенств (4) следует, что $r_a \geq 1, r_{a-1} \geq 2, r_{a-2} \geq 3, \dots$ и вообще $r_i \geq q - i + 1$. Позному $m \geq 0$, т. е. $(x, y)$ — вектор вида (3), что и требовалось.

В геометрических терминах утверждение 6) означает, что число

$$N(x, y)$$ зависит лишь от номера $m$ парабoles и не зависит от номера $q$ точки на параболе.

Пусть $T(m, q)$ — множество представлений вектора (3) в виде суммы различных образующих и $t(m, q)$ — число таких представлений. Задача будет решена, если мы докажем, что для любого целого $q$ имеет место равенство $t(m, q) = t(m, q - 1)$ (это и значит, что $t(m, q)$, а вместе с ним $N(x, y)$, не зависит от $q$). Мы отложенные выше множество $T(m, q)$ с множеством таких пар последовательностей, удовлетворяющих неравенствам (4), что $r_1 + \dots + r_a + s_1 + \dots + s_b = m + q(q + 1)/2$ при $q = a - b$. Такую пару мы будем записать в виде $(r_1, \dots, r_a | s_1, \dots, s_b)$.

Рассмотрим отображение $\varphi$ множество $T(m, q)$ в множество $T(m, q - 1)$, заданной формулой
$$\varphi(r_1, \dots, r_a | s_1, \dots, s_b) = \begin{cases} (r_1 - 1, \dots, r_a - 1 | s_1 + 1, \dots, s_b + 1, 0), \\ \text{если } r_a > 1, \\ (r_1 - 1, \dots, r_{a-1} - 1 | s_1 + 1, \dots, s_b + 1), \\ \text{если } r_a = 1. \end{cases}$$

Упражнение 6. Проверьте, что $\varphi(r_1, \dots, r_a | s_1, \dots, s_b) \in T(m, q - 1)$.

Чтобы доказать, что $\varphi$ — взаимно однозначное отображение, построим обратное отображение $\psi$: $T(m, q - 1) \rightarrow T(m, q)$, прочитав правило, заданное $\varphi$, следваем:

$$\psi(r_1, \dots, r_a | s_1, \dots, s_b) = \begin{cases} (r_1 + 1, \dots, r_a + 1, 1 | s_1 - 1, \dots, s_b - 1), \\ \text{если } s_b > 0, \\ (r_1 + 1, \dots, r_a + 1 | s_1 - 1, \dots, s_b - 1), \\ \text{если } s_b = 0. \end{cases}$$

Построенные отображения взаимно обратны, поэтому $\varphi$ — взаимно однозначное соответствие. Значит, $t(m, q) = t(m, q - 1)$, что и утверждалось.

Чтобы научиться вычислять значения $N(x, y)$, установить между числами $t(m, q)$ и $p(m)$.

Утверждение: $t(m, q) = p(m)$.

Мы уже знаем, что $t(m, q) = t(m, 0)$, поэтому достаточно доказать, что $t(m, 0) = p(m)$. Воспользовемся простым и полезным графическим средством, называемым «диаграммой Юнса раз-
