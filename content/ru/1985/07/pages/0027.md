---
issue: kvant_1985_7
year: 1985
number: "7"
page_index: 27
page_label: "25"
lang: ru
source: kvant_digital
source_scan: kvant_1985_7
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 391f5ff2d452cb11d23ce733e92b5ec10f3d1a5f1d640273efb7f41045e71f47
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 25⟧

решить уравнение

$$a(x)f(x) + b(x)f(1-x) = c(x),$$

(*)

где $a, b, c$ — неокоторые функции. Ясно, что выполняя подстановку $x \rightarrow 1-x$, мы получим уравнение

$$a(1-x)f(1-x) + b(1-x)f(x) = c(1-x),$$

(**)

которое вместе с уравнением (*) образует линейную систему относительной функции $f(x)$ и $f(1-x)$. Дальше решение будет развивать так же, как и при решении задачи 1.

Во втором рассмотрении примере мы делали подстановки

$$x \rightarrow \frac{x-1}{x+1}, x \rightarrow -\frac{1}{x}, x \rightarrow \frac{x+1}{1-x},$$

то есть имена дело с функциями

$$g_1(x) = x, g_2(x) = \frac{x-1}{x+1},$$

$$g_3(x) = -\frac{1}{x}, g_4(x) = \frac{x+1}{1-x}.$$

Постотрим, как ведут себя функции $g_1, g_2, g_3, g_4$ по отношению к операции композиции. Составим таблицу 2, аналогично таблице 1 (в пересечении $i$-й строки и $k$-го столбца запишем $g_i \circ g_k$).

Таблица эта симметрина относительно своей диагонали (это значит, что $g_i \circ g_k = g_k \circ g_i$ при любых $k$ и $i$). Кроме того, все функции $g_i$ встречаются в каждой строке и каждом столбце равно один раз, и, наконец, легко заметить, что $g_3 = g_2^2, g_4 = g_3^2, g_1 = g_2^4$. Здесь $g_i^2 = g_2 \circ g_2 \circ \dots \circ g_2$.

Таким образом, система функции $G = \{g_1, g_2, g_3, g_4\}$ обладает следующими свойствами: а) она замкнута относительно композиций; б) среди этих функций есть тожественное отображение $g_1(x) = x$; в) у каждой из функций $g_i$ есть обратная $g_i^{-1}: g_1^{-1} = g_1, g_2^{-1} = g_4, g_3^{-1} = g_3, g_4^{-1} = g_2$.

Теми же свойствами обладает и система функции $G = \{g_1, g_2\}$ из примера 1.

Если бы теперь нам предложили решить любое функциональное уравнение вида

$$a(x)f(x) + b(x)f\left(\frac{x-1}{x+1}\right) + c(x)f\left(-\frac{1}{x}\right) + d(x)f\left(\frac{x+1}{1-x}\right) = h(x),$$

(***)

мы сделали бы это, выполив замены $x \rightarrow g_2(x), x \rightarrow g_3(x), x \rightarrow g_4(x)$, после чего пришли бы к линейной системе. Для примера запишем, что получится из (***) после замены $x \rightarrow g_2(x)$.

При этом $g_2(x) \rightarrow g_3(x), g_3(x) \rightarrow g_4(x), g_4(x) \rightarrow g_1(x)$, так что получится уравнение

$$a\left(\frac{x-1}{x+1}\right)f\left(\frac{x-1}{x+1}\right) + b\left(\frac{x-1}{x+1}\right)f\left(-\frac{1}{x}\right) + c\left(\frac{x-1}{x+1}\right) \cdot f\left(\frac{x+1}{1-x}\right) + d\left(\frac{x-1}{x+1}\right)f(x) = h\left(\frac{x-1}{x+1}\right).$$

Теперь даим следующее определение.

О пределене. Произвольное множество $G$ функции, определяемых на некотором множество $M$, называется *epynnoi* относительно операции $\circ$, если оно обладает теми же свойствами, что и система $(g_1, g_2, g_3, g_4)$, то есть

1. Для любых двух функции $f \in G, g \in G$ их композиция $f \circ g$ тоже принадлежит $G$.
2. Функция $e(x) = x$ принадлежит $G$.
3. Для всякой функции $f \in G$ существует обратная функция $f^{-1}$, которая принадлежит $G$.

Это определение есть частный случай общего определения понятия группы — одного из важнейших понятий современной математики. Подробно и популярино основные понятия теории группы изложены в книге П. С. Александрова «Введение в теории группы» (Библиотека «Квант», вып. 7, М.: Наука, 1980).

Два примера группы функции мы уже видели. Приведем еще несколько примеры.

a) Множество $G$ линейных функций $f(x) = ax + b$, ($a \neq 0, b \in R$);

б) $G = \{x, \frac{1}{1-x}, \frac{x-1}{x}\} \in R\{0, 1\}$;

b) $G = \{x, \frac{1}{1-x}, \frac{x-1}{x}\} \in R\{0, 1\}$;
