---
issue: kvant_1980_9
year: 1980
number: "9"
page_index: 55
page_label: "53"
lang: ru
source: kvant_digital
source_scan: kvant_1980_9
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: f73be361b60e53bd7ecf29b07865651f3890fa014f2f27532f942c7ba7ad0c4f
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 53⟧

(«Квант», 1980, № 1) рисунок нового строку (рис. 1) длиной 100 мм с расстоянием между линейками в 2,5 мм. Допустим, что нижняя линейка должна быть проведена на высоте 10 мм.

До написания программы отметим следующие:

— неразумно рисовать каждую линейку отдельно: надо написать операторы рисования одной линейки, а затем несколько раз повторить их в цикле;

— поскольку одна линейка от другой отличается только высотю, в качестве переменной цикла имеет смысл выбрать координату y;

— при каждом повторении цикла его переменная изменяется на постоянную величины Δy=2,5, которое называют шадом;

— первое выполнение повторющся в цикле операторов (рисование первого сизу линейки) осуществляется при y₀=10; значит, перед началом цикла нужно обеспечить, чтобы величина y получила начальное значение 10; присваивание начального значения переменной цикла называют ее инцициализацию; повторяющиеся операторы цикла называются телом цикла;

— по определению условий, записываемого после слова ПОКА («Квант», 1980, № 1), цикл завершает, когда изменяется в ходе выполнения цикла переменная y достигает своей еганицы yₖ, которая вычисляется по формуле

$$y_k = y_0 + \Delta y \cdot (n-1),$$

(1)

где n — число повторений цикла. В нашем примере yₖ=10+2,5(5—1)=20.

Программа рисования нового стана имеет вид:

$$10 > Y;$$

$$\Pi(0,Y);\lambda(100,Y);Y+2.5 > Y$$

ВСЕ

Несложное в нашем примере вычисление границ переменной в других задачах может оказаться более трудоемким. Во всяком случае, каждое «ручное» вычисление при составлении программы — это источник возможных ошибок. Когда число повторяющихся так:

$$1 > \Pi(0,Y);10 > Y;$$

$$\Pi(0,Y);\lambda(100,Y);Y+2.5 > Y;$$

$$\Pi(0,Y);\lambda(100,Y);Y+2.5 > Y;$$

СЧЕТЧИК+1 > СЧЕТЧИК

ВСЕ;

$$1 > \Pi(0,Y);10 > Y;$$

$$\Pi(0,Y);\lambda(100,Y);Y+2.5 > Y;$$

СЧЕТЧИК+1 > СЧЕТЧИК

Рис. 1.

Рис. 2.

Рис. 3.

рений заранее известно (как в нашем примере), можно избавиться от таких вычислений, используемых в программе счетчик числа повторений, который будет играть роль переменной цикла. Поэтому надо забать инцициализировать счетчик в начале выполнения программы. Нашу программу можно тогда переписать в следующем виде:

$$1 > \Pi(0,Y);10 > Y;$$

$$\Pi(0,Y);\lambda(100,Y);$$

(3)

$$Y+2.5 > Y;$$

СЧЕТЧИК+1 > СЧЕТЧИК

ВСЕ

Задание 9.1. Написите два варианта программы, рисунок домик с забором, составленный из 12 досок. Размеры (в мм) деталей изображения отменены на рисунке 2.

А теперь вернемся к нашей нотной строке и чут-чут условим задачу: нотную строку требуется разметить такими — вертикальными черточками поперек потных лиnek (рис. 3). На строке размешается 8 тактов. Значит, размер поля между тактовыми чертями — 100/8 = 12,5 мм. Программа записывается так:

$$1 > \Pi(0,Y);10 > Y;$$

$$\Pi(0,Y);\lambda(100,Y);$$

(4)

$$Y+2.5 > Y;$$

СЧЕТЧИК+1 > СЧЕТЧИК

ВСЕ;

$$1 > \Pi(0,Y);10 > Y;$$
