---
issue: kvant_1987_1
year: 1987
number: "1"
page_index: 16
page_label: "14"
lang: ru
source: kvant_digital
source_scan: kvant_1987_1
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 5616fbd4cac77e4be24fd09f21ffccf5bf8f752952c4d2c0d3a499795952a24b
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 14⟧

Рациональная параметризация конечных секений

Для нахождения рациональных точек, лежащих на окружности с уравнением $x^2 + y^2 - 1 = 0$, мы выведили формулу (6). Она устанавливает взаимно однозначное соответствие между параметром $k$, принимающим всевозможные действительные значения, и точками окружности (за исключением точки $(-1; 0)$), причем координаты $(x; y)$ точек, лежащих на окружности, являются рациональными функциями от $k$. Возникает вопрос: можно ли аналогичным образом задавать точки, лежащие на других кривых, скажем на эллипсе, параболе или гиперболе? (Эллипс, парабола и гипербола являются конечными секениями; отсюда называется этого раздела.)

Для ответа на этот вопрос рассмотрим кривую на плоскости, заданную уравнением $K(x, y) = 0$, где $K(x, y)$ — квадратный многочлен от $x$ и от $y$. Точки, лежащие на параболе, эллипсе и гиперболе, как раз удовлетворяют такого рода соответствию. Пусть $(x_1; y_1)$ — какая-нибудь фиксированная точка такой кривой. Проведем через эту точку прямую с угловым коэффициентом, равным $k$ (рис. 3). Будем искать точки $(x; y)$ пересечения кривой и прямой. координаты $(x; y)$ этих точки удовлетворяют системе уравнения
$$\begin{cases} K(x, y) = 0, \\ y - y_1 = k(x - x_1). \end{cases}$$

Решая эту систему относительно $(x; y)$ так же, как мы это делали раньше для окружности, выразим координаты $(x_2; y_2)$ второй точки пересечения

прямой $y = y_1 + k(x - x_1)$ с кривой $K$ через параметр $k$. Легко проверить, что получится выражение вида
$$\left(x_2; y_2\right) = \left(\frac{A(k)}{C(k)}; \frac{B(k)}{C(k)}\right),$$
где $A(k), B(k), C(k)$ — многочлены степени не выше 2 от параметра $k$. Эта формула задает рациональную параметризацию кривой $K$, т. е. выражает координаты точки на кривой через рациональные функции от одного параметра $k$.

Формула (***), как и формула (**), позволяет нахождение целые точки на кривой $K$. Однако сейчас нас интересует другое — совсем неожиданное — применение этой формулы, к которой мы переходим.

Применение к вычислению интегралов

Оказывается, что такие безнадежные с виду интегралы, как
$$I = \int \frac{dx}{\sqrt{x^2 + 3x - 4}},$$
берутся, если воспользоваться рациональным параметризацию подходящие выбранной кривой. Именно, если в данной случае взять кривой
$$K(x, y) = y^2 - (x^2 + 3x - 4),$$
заметить, что точка $(x_1; y_1) = (-4; 0)$ лежит на кривой
$$y(x) = \sqrt{x^2 + 3x - 4}$$
и рассмотреть всевозможные прямые
$$y = k(x + 4),$$
то получится следующая параметризация (проделяйте выкладки самостоятельно!):
$$\left(x; y\right) = \left(\frac{1 + 4k^2}{1 - k^2}; \frac{5k}{1 - k^2}\right).$$

Тогда $dx = \frac{10k}{(1 - k^2)^2} dk$, и поэтому (после сокращения) получим
$$I = \int \frac{dx}{\sqrt{x^2 + 3x - 4}} = \int \frac{dx}{y(x)} = \int \frac{2dk}{1 - k^2}.$$

Послений интеграл уже легко вычислить:
$$\int \frac{2dk}{1 - k^2} = \int \frac{dk}{1 - k} + \int \frac{dk}{1 + k} = \ln |1 + k| - \ln |1 - k| + C = \ln \left| \frac{1 + k}{1 - k} \right| + C.$$
