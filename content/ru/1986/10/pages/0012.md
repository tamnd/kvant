---
issue: kvant_1986_10
year: 1986
number: "10"
page_index: 12
lang: ru
source: kvant_digital
source_scan: kvant_1986_10
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 5309161a058230f8d742574834ecb0fab00feb1fe2248497763a792018d08e29
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio none⟧

Допустим противное, что произведение при-
митивных многочленов
$$L(x) = b_0 + b_1(x) + \dots + b_ix^l,$$
$$Q(x) = c_0 + c_1x + \dots + cmx^m$$
есты многочлен
$$P(x) = a_0 + a_1x + \dots + anx^n,$$
не является примитивным. Пусть тогда $p$ — один из простых делителей его коэффициентов. Пусть $b_i$ — самый младший коэффициент $L(x)$, не делится на $p$, $0 \leq i \leq l$, а $c_j$ — самый младший коэффициент $Q(x)$, не делится на $p$, $0 \leq j \leq m$. Такие коэффициенты обязательно найдутся, иначе $L(x)$ и $Q(x)$ не были при-
митивными. Коэффициент при $x^{i+j}$ в произ-
ведении
$$L(x)Q(x)$$
имеет вид
$$\dots + b_{i-1}c_{j+1} + \dots + b_{i+1}c_{j-1} + \dots$$
и не делится на $p$, так как все слагаемые этой суммы, кроме одного $b_i c_j$, делятся на $p$. С другой стороны, эта сумма равна $a_{i+j}$ — коэффициент многочлена $P(x)$ и должна делиться на $p$. Попученное противоречие доказывает лемму.
И наконец,
Доказательство ТЕОРЕМЫ,
которая утверждает, что любой многочлен $P(x)$, имеющий общий корень $a$ с неприводимым многочленом $Q(x)$, делится на него после умножения на некоторое целое $d \neq 0$.

Разделим многочлен $P(x)$ на $Q(x)$ «с остатком»:
$$\frac{a_n x^n + \dots + a_0}{a_n x^n + \dots} \left| \frac{c_m x^m + \dots + c_0}{a_n \frac{c_m}{c_m} x^{n-m} + \dots} \right|$$
В результате получим, что
$$P(x) = L_1(x) \cdot Q(x) + R_1(x).$$
Частное $L_1(x) = \frac{a_n}{c_m} x^{n-m} + \dots$
и остаток $R_1(x) = r_{m-1} x^{m-1} + \dots$
будут многочленами от $x$ с коэффициентами — рациональными числами.
Если $P(x)$ разделится на $Q(x)$ без остатка: $R_1 \equiv 0$, то все уже доказано, либо тогда
$$P(x) = L_1(x) \cdot Q(x) = \frac{1}{d} L(x) \cdot Q(x),$$
где $d$ — общий знаменатель коэффициентов многочлена $L_1(x)$.
Если же $R_1(x) \neq 0$, то его степень по $x$ будет строго меньше степени делителя $Q(x)$, а число $a$, будучи одновременного корнем $P(x)$ и $Q(x)$, является также корнем многочлена $R_1(x)$:
$$R_1(\alpha) = P(\alpha) - L_1(\alpha) \cdot Q(\alpha) = 0.$$
Разделив $Q(x)$ на $R_1(x)$, получим и
остатке новый многочлен $R_2(x)$ с аналогичным свойством. Его степень будет строго меньше степени $R_1(x)$, а значит, и степени $Q(x)$.
Послеовательно деля $Q(x)$ на получающейся остатки $R_1(x)$, $R_2(x)$, ..., $R_k(x)$, приедем либо к противо-
речению:
$$R_k(x) \equiv c \neq 0,$$
и $a$ не может быть корнем $R_k(x)$, либо к тому, что $Q(x)$ разделится на $R_k(x)$ без остатка: $Q(x) = L_k(x) \cdot R_k(x)$.
Приводя рациональные коэффициенты многочленов $L_k(x)$, $R_k(x)$ к общему знаменателю и выносая за скобки общие делители числителя, перепишем это равенство в виде
$$Q(x) = \frac{a}{b} [L(x) R(x)],$$
где $a/b$ — несократимая дробь, а $L(x)$, $R(x)$ — примитивные многочлены.
Остается показать, что числовой коэффициент $a/b$ в последнем разложении есть целое число, то есть что $b = \pm 1$. Допустим противное, и пусть $p$ — простой делитель числа $b$. Тогда из равенства
$$bQ(x) = a[L(x) R(x)]$$
видим, что все коэффициенты правой части делятся на $p$; $a$ не может делиться на $p$ — в противном случае дробь $a/b$ сокращима. Значит, на $p$ делятся все коэффициенты многочлена $L(x)R(x)$, что невозможно по лемме Гаусса.
Значит, многочлен
$$Q(x) = [\pm aL(x)] \cdot R(x)$$
приводим, что противоречит условия ТЕОРЕМЫ и тем самым полностью ее доказывает.
Давно канули в небытые древние греки. Миновала средневековая неразбериxa. Уходил и XIX век, классический век математики. За ним надвигался наш век — век XX-й...
