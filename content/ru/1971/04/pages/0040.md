---
issue: kvant_1971_4
year: 1971
number: "4"
page_index: 40
page_label: "38"
lang: ru
source: kvant_digital
source_scan: kvant_1971_4
extraction: vision
extraction_model: zai-org/GLM-OCR
content_sha256: 4609ac1fb5688930cc61a459e85523def4f9633591e259880570b0e705a3721f
prompt_sha256: 80272cc3e8fc7722abb690e828c80eeb2cd3bc03e1e75b969b3860f3f5d36166
---

⟦folio 38⟧

Рис. 5.
сумму дуг, лежащих в углублениях между двумя соседними монетами, в которых $M$ не попадает. В каждом из $n$ углублений сумма двух таких дуг равна $2\pi/3$ при $k=1$ (рис. 4a) и $2\arccos \frac{1}{k+1}$ в общем случае (рис. 4, 6). Итак, сумма дуг, по которым прокатится монета $M$, равна $\pi(n+2)-2\pi n/3$ (в общем случае $\pi(n+2)-2n\arccos \frac{1}{k+1}$). Чтобы узнать искомое число оборотов, нужно умножить эту величины на 2 (в общем случае, на $1+1/k$) и разделить на 2π.

Ответ. $\left(\frac{n}{3} + 2\right)$
оборота при $k=1$;
$\frac{k+1}{2k}\left(n - \frac{2}{\pi}n\arccos \frac{1}{k+1} + 2\right)$
оборота в общем случае ($n \geqslant 3$).

Рис. 6.

В этом решении мы использовали (подсчитывая «потери» в углублениях между монетами) то, что монета $M$ прокатывается по всем $n$ монетам подряд, без исключения. Как правило заметил читатель А. Вировянский из Горького, прилавший нам решение, это условие необходимо для того, чтобы задача имела определенный ответ. Для цепочки с «узkimmi просветами» (см. рис. 5), разумеется, нельзя только по числу $n$ узнать, на сколько повернется монета $M$. Достаточный, но не необходимым условием, чтобы задача была разрешимой, является следующее: многоуровольник $O_1$, $O_2...O_n$ випуклы.

Подумайте, как получить ответ, если вместе замкнутой цепочки имеется просто одна монета, две касающиеся друг друга монеты и вообще незамкнутая цегочка из $n$ монета $O_1,...,O_n$, в которой $O_k$ касается $O_{k+1}$, расположенных так, что $M$ может прокатыться туда и обратно по всем монетам в таком порядке:

$O_1$, $O_2,...,O_{n-1}$, $O_n$, $O_{n-1},...,O_1$ (рис. 6) (Указание. Достаточно в написанных выше формулах замкнуть $n$ на $2n-2$.).

M30

Докажите, что $N$ точек на плоскости всегда можно покрыть несколькими непересекающими кругами, сумма диаметров которых меньше $N$ и расстояние между любыми двумя из которых больше 1. (Под рассмотрением между двумя кругами поиска расстояние между их близкойшим точками.)

Нам понадобится следующее очевидная lemma.

Если два круга диаметров $a'_1$ и $a'_2$ пересекаются (имеют общую точку), то их можно заклонить в один круге диаметра не больше $a'_1+a'_2$ (рис. 7, а, 6).

Построим круг с центром в каждой из данных $N$ точек, имеющий радиус $a$ (а несколько больше $1/2$; точнее значение $a$ выберем ниже). Если среди этих кругов оказываются пересекающими, то, пользоваться lemмою, заменим какие-либо два пересекающих круга (все равно какие) одним покрывающим их кругом. Если среди полученных кругов еще есть пересекающие, снова воспользуемся lemмою, и т. д. Пусть вообще есть какая-то система кругов, которые: 1) прокрывают все данные точки вместе с кругами радиуса $a$ с центрами в этих точках и 2) имеют сумму диаметров не больше $N \cdot 2a$. Тогда если среди них есть пересекающие, то мы можем
